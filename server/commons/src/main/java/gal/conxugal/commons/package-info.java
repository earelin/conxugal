/**
 * Shared, dependency-free utility code (validation helpers and similar pure functions) used
 * across the server's other modules. Depends on nothing outside itself; carries no transport,
 * persistence, DI or business-rule concerns.
 */
package gal.conxugal.commons;
