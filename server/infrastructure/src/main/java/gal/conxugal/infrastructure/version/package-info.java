/**
 * Access to the build-stamped application version, shared by every part of {@code
 * infrastructure} that has to identify this build — the system-status probe and the outbound
 * HTTP client's {@code User-Agent}.
 */
package gal.conxugal.infrastructure.version;
