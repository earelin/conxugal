/**
 * Money: the {@link gal.conxugal.domain.money.Money} amount every stored figure is held as,
 * shared by any aggregate that carries one and by whatever sums them.
 *
 * <p>{@code @NullMarked}: every type, field, parameter and return value in this package is
 * non-null unless explicitly annotated {@link org.jspecify.annotations.Nullable}.
 */
@NullMarked
package gal.conxugal.domain.money;

import org.jspecify.annotations.NullMarked;
