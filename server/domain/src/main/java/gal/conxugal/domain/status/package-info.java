/**
 * System status: the {@link gal.conxugal.domain.status.SystemStatus} snapshot and the
 * {@link gal.conxugal.domain.status.SystemStatusProbe} port that assembles the current one.
 *
 * <p>Nothing here is stored: a snapshot is a value assembled on demand and handed to whoever
 * asked for it.
 *
 * <p>{@code @NullMarked}: every type, field, parameter and return value in this package is
 * non-null unless explicitly annotated {@link org.jspecify.annotations.Nullable}.
 */
@NullMarked
package gal.conxugal.domain.status;

import org.jspecify.annotations.NullMarked;
