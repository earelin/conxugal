/**
 * Time: the {@link gal.conxugal.domain.time.Clock} port, shared by any domain use case
 * that needs to read the current instant.
 *
 * <p>{@code @NullMarked}: every type, field, parameter and return value in this package is
 * non-null unless explicitly annotated {@link org.jspecify.annotations.Nullable}.
 */
@NullMarked
package gal.conxugal.domain.time;

import org.jspecify.annotations.NullMarked;
