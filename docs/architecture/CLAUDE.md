# Architecture Decision Records

ADR authoring — Nygard format, frontmatter, the significance test, and
supersession rules — is owned by the `create-adr` skill. This repo adds no
overrides; follow the skill.
